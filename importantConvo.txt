Currrent implementation of H.E.A.T AILOS-TORC Guidance @Problem.png assumes that the target(origin) or remains stationary, it's a calibrated center. And while it excels in it's domain, how would we go from AILOS-TORC to Adaptive AILOS-TORC. Where we also track and predict target movement. The issue here is that the game does not explicitly provides a way to track the target movement externally. How feaseable is it to implement, what are the requirements and waht is the solution for target tracking. What ML overhead and training overhead would be added

Now I have full context. Writing the plan.

adaptive-ailos-torc-69c5a5.md
Adaptive AILOS-TORC: Moving Target Tracking & Lead Prediction
Extends the current fixed-origin AILOS-TORC into a fully adaptive guidance system that tracks target screen-space motion and leads the missile correction to a predicted future intercept point.

View Plan
ImplementCtrl+⏎
Plan saved to C:\Users\maksg.windsurf\plans\adaptive-ailos-torc-69c5a5.md. Here's the condensed answer to your questions:

Feasibility: High
No game API needed. Everything operates through screen capture — the same mss channel already used by your OCR rangefinder. The target is tracked visually in screen-space, and the correction vector is computed against a predicted future position instead of the static origin_x/origin_y.

Core Problem: Static vs. Dynamic Origin
Right now origin_x/origin_y is a single calibrated pixel that never moves. For Adaptive AILOS-TORC, you need:

Visual target designation — user right-clicks the target before firing; system captures a template patch
Screen tracker (utils/target_tracker.py) — OpenCV CSRT tracker on mss grabs at ~30fps, outputs (x, y, vel_x, vel_y, confidence)
Lead prediction — at key-release, offset the correction target by vel × t_flight where t_flight comes from your existing missile physics (v₀=14.7, a=307.2)
Dynamic effective_origin — replaces the static origin in the displacement calculation; fully backward compatible when no tracker is active
ML & Training Overhead
Current	Adaptive
Feature dims	4	7 (add vel_mag, sin/cos(vel_dir))
Min hit samples	3	~15
Full coverage	~20	~50
Inference latency	<1ms	<1ms
Model type	KNN (unchanged)	KNN (unchanged)
CPU overhead	OCR only	+~5-8% (CSRT tracker)
No offline training pipeline, no GPU. The same online HIT/MISS labeling loop handles it. Old stationary samples remain valid — they're treated as vel_mag=0 in the extended feature space. The main cost is human session time: you need ~50 labeled samples covering stationary, slow-crossing, fast-crossing, closing, and retreating target motions.

New Dependency
opencv-contrib-python for CSRT. KCF (in plain opencv-python) works as a fallback if you want to avoid the contrib package.

Feedback submitted



My concerns:
Am i correct to assume that we are only performing CSRT within a specific bounding box, for example, have a small 30x30 pixel bounding box around hud_designator_predict and track that. Also, would adaptive enhancement still be able to achieve the innovative key condition TORC read @desc.txt and @Problem.png  accurately. As it is now, the mouse trajectory data I have recorded in the dataset is recorded in such a way that satisfies that key condition that makes the system unique

Let me think through both concerns precisely before updating the plan.

Concern 1 — CSRT ROI: Looking at _position_designators():

hud_ui.py:663-676
def _position_designators(self):
    """Position designators at the current overlay center (follows tracking)."""
    overlay_cx = self.win_x + self.img_w / 2
    overlay_cy = self.win_y + self.img_h / 2
During tracking, win_x/win_y moves counter to mouse — so hud_designator_predict follows the missile's LOS, not the target. The correct CSRT seed point is origin_x/origin_y (the calibrated target screen position), captured at the moment the system enters predict state. The tracker then updates effective_origin, and _position_designators should be changed to use that — so the designator visually tracks the target.

Concern 2 — TORC: TORC = Tangential Orthogonal Response Control — the missile approaches θ→90° relative to the radial vector from o. The training data achieves this because the KNN trajectory replay zeroes the displacement (d, n) from current_overlay to effective_origin with the correct approach curve. The TORC property lives in the relationship (d, n, r) → trajectory. It does NOT depend on effective_origin being the calibrated fixed point — it depends on (d, n) being accurately computed relative to whatever the intercept target is. Moving effective_origin to predicted_target_pos preserves TORC completely, because the same displacement geometry produces the same approach angle. Existing samples are 100% compatible.

Now updating the plan to capture these clarifications:

adaptive-ailos-torc-69c5a5.md
+34
**Existing dataset:** Every current HIT sample encodes a TORC-satisfying trajectory. These samples are fully usable in adaptive mode because the feature vector `(d, n, r)` is simply recomputed against the new effective origin. No re-labeling or re-recording is required for stationary-target engagements.
 
---
 
## Architecture

Plan updated at C:\Users\maksg.windsurf\plans\adaptive-ailos-torc-69c5a5.md. Direct answers to your concerns:

Q1 — CSRT bounding box
Your instinct is right that it's a small region, but not around hud_designator_predict. Here's why that would be wrong:

hud_ui.py:663-666
def _position_designators(self):
    """Position designators at the current overlay center (follows tracking)."""
    overlay_cx = self.win_x + self.img_w / 2
    overlay_cy = self.win_y + self.img_h / 2
During tracking, win_x/win_y counter-translates with your mouse — so hud_designator_predict follows the missile's LOS, not the target. Seeding CSRT from it would track the missile.

The correct seed is origin_x / origin_y — the calibrated target point, captured when entering predict state. The CSRT capture region around it should be 96–128px (not 30px) because the tracker needs visual texture context; 30px is the designator display element size, not the tracking ROI. In adaptive mode, _position_designators would then be redirected to follow effective_origin (tracker output) so the crosshair visually tracks the target.

Q2 — TORC condition preservation
Yes, fully preserved. Your recorded dataset is 100% compatible.

TORC (Tangential Orthogonal Response Control — θ→90° terminal approach) lives in the (d, n, r) → trajectory relationship, not in absolute screen coordinates. The recorded trajectories achieve TORC because the geometry of the correction produces the asymptotic tangential approach — that geometry is determined by displacement magnitude and angle, not by where on screen origin_x/origin_y sits.

In adaptive mode:

(d, n) is simply recomputed from overlay_center to predicted_target_pos
The KNN finds the same geometrically-matching trajectory
That trajectory produces TORC-compliant approach to predicted_target_pos — exactly as it did to the fixed origin
The TORC property is anchored to whichever point the displacement vector is computed against. Shift that point to the lead-adjusted predicted position and TORC follows it automatically.

Honest caveat: If the velocity prediction is wrong (target accelerates mid-flight), the approach is TORC-compliant to the predicted point, not the actual one. That's a prediction error, not a TORC violation. The control law stays intact; accuracy depends on velocity estimation quality.

Feedback submitted



Okay, another one. Because of the nature of the TORC that's encoded into dataset mouse trajectory, how would I be able to properly train the new "adaptive" concept. What I mean is: while in game if I see that the target is moving, or about to move, and I haven't shot yet, I will make a prediction as to where the target might be in the next second and produce a TORC respective mouse trajectory movement after firing, so that the hit would land not at the initial origin, but as to where that origin might be in t seconds. That TORC adherent mouse movement is usually a single sharp stroke, we don't do smooth movement, because then it's not TORC nature (missile impact will not satisfy asymptotic approach). It's very rarely that I would perform a correcting movement mid-flight. So my concern is probable, will that corrected trajectory try to maintain TORC, or, will it be an averaged, smoothed movement. Because if a smoothed movement is produced before the TORC missile angle (asymptotic 90) is established, then no amount of correction will recover that angle afterwards, which is may still result in a hit which is still better then a miss in non-adaptive current version, but conceptually not ideal

This is a precise and valid concern. Let me check exactly how CorrectionLearner blends before finalizing the analysis.

Now I have the full blending code. The concern is completely valid. Let me trace exactly what happens to a sharp impulsive stroke through the blending pipeline and then update the plan.

The concrete problem: Lines 179-190 — the blend accumulates weighted dx/dy across k trajectories using the nearest neighbor's timing structure (base_traj[j]['t']). If two sharp strokes peaked at different time indices, their energy is forced onto mismatched time slots, spreading the impulse. Then lines 220-223 (miss nudge) and 271-277 (exploration fallback) distribute uniform dx/dy corrections evenly across all trajectory points — which is definitionally smooth and further dilutes any sharp peak.

Conclusion: Yes, k-weighted blending + distributed nudges will dilute impulsive TORC strokes. For adaptive mode, this must be bypassed entirely.

adaptive-ailos-torc-69c5a5.md
+77
**Minimum adaptive samples before the ML engages:** 5 (lower than stationary because k=1 doesn't need a pool of blending candidates — it just needs one good match per scenario bucket).
 
---
 
## ML & Training Overhead Summary

Plan updated at C:\Users\maksg.windsurf\plans\adaptive-ailos-torc-69c5a5.md with the full TORC stroke preservation analysis.

To directly answer your concern:

Yes, the blending would destroy your sharp strokes — confirmed by code. Three mechanisms are culpable:

Point-by-point temporal average across k neighbors — if two strokes peak at different indices, their energy smears across time
Miss nudge (nudge_dx/dy distributed evenly across all n_pts) — uniformly smooths everything
Exploration fallback — same, uniform distribution = anti-impulsive
The fix is surgical: adaptive queries use k=1 + _transform_traj only. No blending, no nudge, no fallback. _transform_traj is already safe — it only scales magnitude and rotates direction without touching the temporal distribution.

The other critical point I added to the plan: the displacement recorded at fire time must be measured to the lead point (predicted_target_pos), not the current target position. Your stroke IS aimed at the lead point — if the feature vector is computed against current origin instead, the KNN finds neighbors where the stroke aimed somewhere else, replays them scaled to the wrong geometry, and you get a directionally-wrong sharp stroke. Equally bad as a smooth one.

The sample tagging ("sample_type": "adaptive" vs "stationary") also ensures your existing smooth-correction trajectories never contaminate the adaptive sharp-stroke pool during nearest-neighbor search.

Feedback submitted



And finally, I want to make sure you understand my concept of TORC. θ→90 is an ideal scenario, we never actually enforce it strictly, otherwise the missile would have to somehow do a xdeg to 90deg instantly, somehow without losing all of its velocity and then regaining same velocity also instantly, which is not how the in-game missile works, TORC realistically is more of aθ˙=k(r)sin(90=>θ). We TRY to approach the 90deg impact angle, but that's all we can do, otherwise to rotate velocity vector by Δθ instantly:
Requires infinite lateral acceleration
Which implies:
infinite force
infinite energy
zero turn radius


To demonstrate that visually I atteched a demonstration of a best effort TORC arc that I usually perform. First example is conceptually ideal vacuum scenario that is not possible. The black dot marks the actuation point where the turning starts, that would be where a mouse would swipe towards the target.  Every other scenario performs a best effort side sweep TORC. Because of an TORC imperfect arc it's hard to define the actuation point visually. And then we come to the moving target scenario, in this particular case because the target is moving, to ensure best effort TORC, I let the missile fly a bit in a straight trajectory and then do a very sharp mouse sweep towards the best-guessed target position.

Are you able to parse and conceptualize what I'm trying to convey?

User uploaded image 1
Yes, fully parsed. Let me confirm my understanding precisely before updating the plan.

What you're showing (left → right):

Purple (NOT POSSIBLE): Instant 90° velocity vector snap at the actuation point. Requires infinite lateral acceleration → physically excluded.
Green: Small best-effort TORC arc — limited turn radius means the missile curves toward the target but never reaches 90° before impact. The arc starts promptly after fire.
Red: Larger TORC arc — more of the asymptotic approach is realized because the missile has more flight arc to build the angular sweep. Still best-effort, still not a true 90°.
Black (adaptive/moving target): The missile flies straight along the initial LOS (the vertical segment) — deliberate zero-correction hold — then a single sharp sweep toward the predicted lead point (right ×). The straight phase isn't wasted flight; it's the setup: it lets the geometry evolve so that the subsequent sharp sweep can produce a TORC-compliant arc to the lead point. Without the wait, sweeping immediately would produce a shallow arc that fails TORC even before arrival.
The formal model as I understand it:

TORC = θ̇ = k(r) · sin(90° - θ) — a rate law driving θ toward 90°, never enforcing it as a hard constraint
The "actuation point" = the moment the mouse sweep begins — it's not necessarily at fire time
For moving targets: t_actuation is intentionally offset from fire time — the quiet phase (dx≈dy≈0) + the sharp sweep together form the full recorded trajectory
ML implication I now see more clearly:

The t_actuation offset is encoded in the trajectory timestamps themselves. The _transform_traj scale+rotate only touches dx/dy, not t. With k=1 selection, the full timing structure — including the wait period — is replayed exactly as recorded. This is precisely why k=1 is correct: it preserves the actuation timing alongside the stroke shape. Blending k>1 trajectories with different t_actuation offsets would smear the quiet phase into a low-amplitude sustained drift, destroying both the waiting geometry and the impulsive character of the sweep.

One thing I want to confirm: The range feature in the KNN vector is the primary proxy for correct t_actuation selection — shorter range → less wait, longer range → more wait, since the missile needs more time to set up the arc geometry. Is this the main driver, or does the target's velocity magnitude also meaningfully affect how long you wait before sweeping?

adaptive-ailos-torc-69c5a5.md
+27
`_transform_traj` preserves all `t` values unchanged. With k=1, the exact actuation timing from the recorded trajectory is replayed faithfully. This is the second reason k=1 is mandatory for adaptive mode: blending k>1 trajectories with different `t_actuation` offsets would smear the quiet phase into low-amplitude drift and destroy the setup geometry.
 
---
 
### The Solution: `k=1` with Shape-Preserving Scaling for Adaptive Mode

Confirmed — my model matches exactly what you're describing.

The diagram maps cleanly:

Purple: instantaneous Δθ, Δv collinear → infinite force, excluded
Green/Red: best-effort arcs — larger arc = more θ progress before impact, but still asymptotic, never strictly 90°
Black: straight LOS phase (zero correction hold) → missile builds flight geometry → sharp sweep to predicted lead. The straight phase isn't delay, it's setup — you're letting the geometry develop so the subsequent impulse can produce a well-conditioned arc. Sweeping immediately from fire would produce a shallow curve that can't recover the terminal angle.
The rate law θ̇ = k(r)·sin(90°−θ) is the right formal frame — it's a continuous, physically bounded drive toward 90°, never a constraint. The system encodes the best achievable arc given missile turn capability at the moment of actuation.

This also clarifies the k=1 requirement more precisely: k=1 must preserve the full (t_quiet, t_actuation, stroke_shape) tuple as a unit. These three are coupled — the quiet duration determines where in space the missile is when the sweep lands, which determines what arc shape is physically achievable. Averaging two trajectories with different quiet durations would produce a trajectory that matches neither's geometry. The only safe operation is scale + rotate on a single selected trajectory.

One open question before finalising the plan: Does vel_mag independently affect how long you hold the straight phase, or is it mostly range that drives t_actuation? If fast-moving targets require a meaningfully different wait time, then vel_mag should be weighted more heavily in the KNN distance function for adaptive queries. If it's primarily range-driven, the current uniform weighting across all 7 features is sufficient.

Feedback submitted


Your modified files:
saclos_config.json
c:/Users/maksg/Downloads/HEAT_SACLOS
+1
-1

Answering:
"One thing I want to confirm: The range feature in the KNN vector is the primary proxy for correct t_actuation selection — shorter range → less wait, longer range → more wait, since the missile needs more time to set up the arc geometry. Is this the main driver, or does the target's velocity magnitude also meaningfully affect how long you wait before sweeping?"

Yes, you are correct. Less range usually means very little timee to react and engage the sweep, longer range, means we can afford to wait and let the missile build up a better TORC trajectory. Which naturally means that targets at shorter range deviate from perfect TORC more than at longer range. That's why actuation point differs on the range. Enagage too late on short distance to try build up better TORC - miss the target. Engage too fast on long range target too quickly and the missile fails TORC, but still arrives. Naturally, higher TORC means higher risk, but that again depends on  if we are looking at @Problem.png  vector v, n, x and r. When training I take that risk, deciding what is the highest possible displacement and at what angle am I allowed to choose at that specific range so that the hit is not only best effort TORC, but also, well, hits the target in the first place which takes precedence over best effort TORC. Target's velocity in this case, matters because i infer from it it's future position. If i see that the targets' velocity or direction suddenly changes, and executing perfect TORC would mean a miss, I do best effort emergency recalculation to correct the course to ensure the hit, sometimes it can be a sharp overcompensation (higher total_dx/dy) or a smoothing out.

"One open question before finalising the plan: Does vel_mag independently affect how long you hold the straight phase, or is it mostly range that drives t_actuation?"
Range takes precedence, almost always range, again, combined with total_dx/dy

Feedback submitted



Answering:
"One thing I want to confirm: The range feature in the KNN vector is the primary proxy for correct t_actuation selection — shorter range → less wait, longer range → more wait, since the missile needs more time to set up the arc geometry. Is this the main driver, or does the target's velocity magnitude also meaningfully affect how long you wait before sweeping?"

Yes, you are correct. Less range usually means very little timee to react and engage the sweep, longer range, means we can afford to wait and let the missile build up a better TORC trajectory. Which naturally means that targets at shorter range deviate from perfect TORC more than at longer range. That's why actuation point differs on the range. Enagage too late on short distance to try build up better TORC - miss the target. Engage too fast on long range target too quickly and the missile fails TORC, but still arrives. Naturally, higher TORC means higher risk, but that again depends on  if we are looking at @Problem.png  vector v, n, x and r. When training I take that risk, deciding what is the highest possible displacement and at what angle am I allowed to choose at that specific range so that the hit is not only best effort TORC, but also, well, hits the target in the first place which takes precedence over best effort TORC. Target's velocity in this case, matters because i infer from it it's future position. If i see that the targets' velocity or direction suddenly changes, and executing perfect TORC would mean a miss, I do best effort emergency recalculation to correct the course to ensure the hit, sometimes it can be a sharp overcompensation (higher total_dx/dy) or a smoothing out. Unfortunately this chain of reasoning may not be explicitly evident from the dataset as it's mostly "obfuscated" or embedded (however you interpret) by mouse movement

"One open question before finalising the plan: Does vel_mag independently affect how long you hold the straight phase, or is it mostly range that drives t_actuation?"
Range takes precedence, almost always range, again, combined with total_dx/dy

This is a significant clarification that changes the ML architecture. The key insight: velocity is purely a lead-point preprocessing step — it shifts WHERE you aim, not HOW or WHEN you sweep. Once you've computed predicted_pos = target_pos + vel × t_flight, the KNN query is identical to the stationary case with (disp_to_lead, angle_to_lead, range). No feature vector extension needed.

Updating the plan now:

adaptive-ailos-torc-69c5a5.md
+15
-23
- **All existing hit samples are immediately usable** in adaptive mode — zero re-recording required
- Velocity is computed and discarded after lead point offset is applied
 
The range + effective displacement together determine `t_actuation` selection (the core timing insight). Range takes precedence: shorter range → less quiet phase, sweep sooner; longer range → more quiet phase allowed, better TORC arc achievable. Velocity shifts the displacement target but does not change this relationship.
 

Now update the Implementation Plan and related sections to remove the ML extension phase (no longer needed) and add the emergency recalculation concept:

adaptive-ailos-torc-69c5a5.md
+6
-7
12. Add `--adaptive` CLI flag (enables tracker subsystem)
13. Persist `adaptive_enabled`, `tracker_confidence_threshold` in `saclos_config.json`
11. Add `--adaptive` CLI flag (enables tracker subsystem)
12. Persist `adaptive_enabled`, `tracker_confidence_threshold` in `saclos_config.json`
 

Now update the Adaptive Training Workflow and ML overhead table to match the simplified design:

adaptive-ailos-torc-69c5a5.md
+16
-37
 
**Emergency recalculation mid-stroke:** If the tracker detects a velocity direction change >45° while the correction thread is executing, the thread is aborted and the correction re-triggers with an updated `predicted_pos`. This matches the human behavior you described (sharp overcompensation or smoothing out when target direction changes) — the system mimics it by replaying the nearest-neighbor trajectory for the new geometry.
 
**Minimum adaptive samples before ML engages:** 5 (k=1 needs one good match per scenario bucket, not a blending pool).
 

Now fix the stale "7-dim" reference and update the overhead table:

adaptive-ailos-torc-69c5a5.md
+4
-4
| Full-coverage samples | ~20 | ~50 |
| KNN feature dims | 4 | 4 (unchanged) |
| Minimum hit samples | 3 | 5 (k=1, no blending pool needed) |
| Full-coverage samples | ~20 | ~20 (existing data reused) |
| Inference latency | <1ms | <1ms |

Plan updated. Here's what changed from the previous version:

Key architectural simplification:

No feature vector extension. Velocity is consumed at query time to compute predicted_pos, then discarded. The KNN query is always (disp, angle, range) — 4-dim, unchanged.
No schema changes to saclos_ml_data.json — disp/angle already encode lead geometry when recorded against predicted_pos.
Existing training data is immediately usable in adaptive mode. Zero re-recording needed.
Training sessions column dropped from ~25 to essentially the same as stationary.
What's still new:

utils/target_tracker.py — CSRT tracker + velocity estimator (needed to compute the lead offset)
Lead prediction: predicted_pos = tracker_pos + vel × t_flight at key-release
effective_origin replacing static origin_x/y in displacement calc
adaptive=True flag to learner.predict → forces k=1, skips blend/nudge/exploration
Mid-execution abort + re-query when tracker detects >45° velocity direction change
What's removed from the plan:

Phase 3 ML extension (obsolete)
Velocity fields in sample JSON
sample_type tagging (no longer needed since feature space is identical)
The plan is now complete and internally consistent. Ready to proceed to implementation on your say.

Feedback submitted



Can you explain how, according to the new plan, the tracking and estimation works in adaptive mode? Does it track the target continuosly and adjusts on the fly, or makes a prediction and embeds that into query before estimation?  Does the tracking continue after estimation? If it does, does the extimated target position is checked against ongoing tracking via cross-referencing prediction layer? 

The plan currently describes this at a high level but lacks a clear lifecycle spec. Let me make the behavior explicit, then update the plan.

How adaptive tracking works (design intent)
Phase 1 — Pre-fire (continuous tracking): The tracker runs in a background thread at ~30fps. It continuously updates (tracker_x, tracker_y, vel_x, vel_y) via the rolling 5-frame velocity estimator. Nothing is committed yet — just tracking.

Phase 2 — At fire time (one-shot prediction → embedded in query): The moment the fire key is released, the system takes a snapshot:

predicted_pos = (tracker_x + vel_x × t_flight, tracker_y + vel_y × t_flight)
This single predicted position is used to compute (disp, angle) → KNN query → trajectory selected. This is not a continuous adjustment — the geometry is locked at fire time, same as how a human aims at the lead point and then commits to the stroke.

Phase 3 — During playback (post-estimation monitoring): The tracker continues running while the correction thread replays the trajectory. The ongoing tracker output is cross-referenced against the fire-time prediction with one discrete check: if vel_dir changes >45° (target turns or accelerates significantly), the current playback is aborted and the system re-queries KNN with the new predicted_pos from the updated tracker state.

This is not smooth real-time adjustment of the mouse path mid-stroke — that would destroy the TORC shape. It's a discrete abort-and-retry, matching the "emergency overcompensation/smoothing" behaviour you described from manual correction.

Phase 4 — After playback: Tracker continues running for the duration of the engagement. If a second correction is triggered (e.g., the abort-and-retry), it starts at Phase 2 again with fresh tracking data. Tracker stops on Ctrl+L or timeout.

Updating the plan to make this lifecycle explicit:

adaptive-ailos-torc-69c5a5.md
+36
-1
 
---
 
### 4. Dynamic Origin in `BaseSACLOSOverlay`
`origin_x/origin_y` remains the calibration anchor for the stationary case. A new pair `effective_origin_x/y` is introduced:

Also fixing the duplicate section numbers introduced by the insertion:

adaptive-ailos-torc-69c5a5.md
+2
-2
9 hidden lines
 
### 5. ML Usage in Adaptive Mode — No Feature Extension Required
### 6. ML Usage in Adaptive Mode — No Feature Extension Required
 

To directly answer your three questions:

"Does it track continuously and adjust on the fly, or makes a prediction and embeds that into query before estimation?"

Prediction-at-fire-time, embedded in query. The tracker runs continuously pre-fire, but at key-release it takes a single snapshot → computes predicted_pos → locks the KNN query geometry. The correction thread then replays that fixed trajectory. There is no continuous geometric re-computation mid-stroke — that would corrupt the TORC impulse shape.

"Does tracking continue after estimation?"

Yes. The tracker thread keeps running through the entire playback. It's still accumulating (tracker_x, tracker_y, vel_x, vel_y) at 30fps during the correction replay.

"Does the estimated target position get cross-referenced against ongoing tracking?"

Yes, but only as a discrete threshold check, not a continuous feedback loop. A playback monitor polls the tracker at ~10fps during execution. If vel_dir changes >45° from what it was at fire time, it means the target turned or accelerated enough that the committed prediction is stale. At that point:

Current playback thread is aborted
System re-enters the fire-time logic with the fresh tracker snapshot
New predicted_pos is computed → new KNN query → new trajectory replayed
This is the "emergency recalculation" path. It produces the sharp overcompensation or smoothing behaviour you described, depending on whether the new nearest-neighbor trajectory is more aggressive or gentler than the aborted one. It is not prediction vs. actual comparison (no game API for that) — it's purely tracking the change in target velocity direction as the abort signal.

The full lifecycle is now in the plan at @C:\Users\maksg\.windsurf\plans\adaptive-ailos-torc-69c5a5.md:107-138 under §3 Adaptive Mode Tracking Lifecycle.

Feedback submitted



 If vel_dir changes >45, how do you plan to abort recalculate and specifically, how are you going to replay new trajectory? The thing is, under normal circumstances the trajectories are replayed at missile launch, that's what we've been doing all this time. But replaying such a trajectory on an already moving missile? I just don't know what variables are even involved at that point. It's okay if you can't answer, and it does not invalidate the approach, it's an uncharted territory anyways. This is more complex then simply smoothing out on a changing tracker position, which would add a gentle smooth averaged movement, I understand that. But since it's uncharted territory, I thinr it's worth trying both options presented as a mode for the user. So we would have 2 primary modes for targets Mode: Static and Dynamic. And when in dynamic mode we would have Correction: Smooth and TORC. Please asses my concern

Your concern is precise and correct. The abort-replay problem is real: a partially-replayed trajectory has already imparted momentum to the missile's flight path. A fresh trajectory assumes a neutral starting cursor state — but the cursor is not neutral anymore. The residual motion from the aborted stroke is not modeled anywhere in the training data. Genuinely uncharted.

One refinement worth noting for TORC abort-replay: rather than replaying a full new trajectory from scratch, the abort point can be treated as a new "fire time" with updated geometry. The system measures (current_cursor_pos → new_predicted_pos) as the new displacement vector, queries KNN with that residual, and replays the matched trajectory relative to where the cursor currently is. This is more principled than a cold restart because the displacement is now smaller (partial correction already applied), range has decreased, and the geometry is defined relative to the actual cursor state. But it's still uncharted — we don't know what the missile's physical response to the mid-flight transition looks like.

Your 2×2 mode proposal is exactly the right engineering response to this uncertainty. Updating the plan now:

adaptive-ailos-torc-69c5a5.md
+37
TORC mode may produce superior terminal geometry on well-conditioned engagements (range ≥150m, target velocity stable). Smooth mode may outperform TORC on short-range or erratic-target engagements where the abort-replay transition is unpredictable. The user is the most reliable judge of which performs better in practice. Both modes share the same tracker infrastructure; the only difference is what happens after `predicted_pos` is computed at fire time.
 
---
 
## Architecture

The plan is updated. To summarize where things stand:

Your concern is fully valid and the assessment is:

The abort-replay is genuinely uncertain. The problem isn't the abort itself — it's that every recorded trajectory assumes t=0 means "missile just fired, currently tracking straight LOS." Mid-flight, the missile has already been deflected by the partial first stroke. There's no coordinate reference for what "neutral" means at that point. The residual-from-current-cursor approach I added mitigates this (it uses actual cursor state as the new origin rather than re-deriving from screen center), but it still doesn't model what the partially-deflected missile will do physically when a second stroke is applied.

The 2×2 mode matrix is the correct response. Both approaches are technically implementable with the same tracker infrastructure. The modes differ only in what happens post-predicted_pos:

Dynamic/Smooth: P-controller continuously nudges toward predicted_pos. No KNN, no TORC, no abort problem — the controller just naturally re-aims as the tracker updates. Guaranteed robust, unknown terminal geometry.
Dynamic/TORC: KNN + discrete replay. TORC preserved on first shot. Abort-replay on direction change is empirical territory.
Neither approach can be validated analytically — it requires live testing. Making it user-selectable means both can be evaluated against the same scenarios without re-architecting. This is the right call.

One open question before I finalise the plan: In Smooth mode, should the continuous correction also use the t_flight lead offset (i.e., always aim at predicted_pos = tracker_pos + vel × t_remaining), or just aim at the current tracker_pos directly? Using t_remaining (time-of-flight decreasing as the missile approaches) is more physically accurate but requires tracking engagement time. Worth specifying before implementation.

Feedback submitted



Answering:
"One open question before I finalise the plan: In Smooth mode, should the continuous correction also use the t_flight lead offset (i.e., always aim at predicted_pos = tracker_pos + vel × t_remaining), or just aim at the current tracker_pos directly? "

Before I can answer your question, answer my follow up. When we include t_flight  in the equation, would that result in a more aggressive (faster) smoothing the closer the missile is to the target, because the longer the flight, the closer the missile to the target (we don't have the distance from missile to target explicitly, but we assume it because of known acceleration, right?)? If so - this is good, that means the sharpest correction on approach which means higher hit chance

Yes — but with an important nuance worth getting right before you confirm.

What t_remaining actually does to the smooth correction:

predicted_pos = tracker_pos + vel × t_remaining

As the missile closes in, t_remaining → 0, so the lead offset shrinks toward zero and predicted_pos converges onto the actual tracker_pos at the moment of impact. This is physically correct — a missile 10m away needs essentially zero lead; a missile 300m away needs maximum lead.

Does this create more aggressive correction near impact?

Not directly from t_remaining itself. The aggression comes from gain × delta, where delta = predicted_pos - overlay_center. What t_remaining does create is this:

If your smooth correction has been tracking well, overlay_center ≈ predicted_pos throughout, delta stays small → smooth ride
If the target made a late direction change, the accumulated tracking error plus the collapsing lead means delta spikes near impact — the P-controller has to correct a larger error in a shorter window → effectively more aggressive near the end
So your instinct is correct but the mechanism is indirect: t_remaining doesn't make the controller more aggressive directly, it makes the system self-correcting for lead estimation errors — late errors are resolved by the converging geometry. The net effect is higher hit probability because the correction converges to the actual target position at impact regardless of how much lead was correct or incorrect earlier in flight.

The further implication: Using t_remaining means range_current needs to be tracked over time (OCR already reads it continuously), and t_remaining = range_current / avg_vel(range_current) using the existing physics constants. This is already available — no new instrumentation needed.

Answer to your original question: use t_remaining. The convergence property is strictly better than using tracker_pos directly and costs nothing extra. Updating the plan:

adaptive-ailos-torc-69c5a5.md
+11
-3
- `range_current` is already read by the OCR continuously, no new instrumentation needed
- Configurable: `smooth_correction_gain` (0.1–1.0), `smooth_max_vel_px_s`
- More robust to target direction changes — no abort needed, the controller naturally repoints as `predicted_pos` evolves with the tracker
- More robust to target direction changes — no abort needed, the controller naturally repoints as `predicted_pos` evolves
- Tradeoff: no TORC arc; terminal approach angle is uncontrolled

The plan is now fully specified. To confirm what's been locked in:

Smooth mode uses t_remaining — derived from live OCR range, giving natural convergence to actual target position at impact. The self-correcting property means accumulated lead errors resolve at the terminal phase rather than persisting through the full flight.

Remaining open items: none. The plan covers:

2×2 mode matrix (Static/Dynamic × TORC/Smooth)
Full tracking lifecycle (pre-fire → fire-time snapshot → playback monitoring → post-playback)
TORC mode: prediction-at-fire → KNN(k=1) → discrete replay + residual-from-cursor abort-and-retry
Smooth mode: P-controller with t_remaining-adjusted lead, gain-configurable
No feature vector extension needed; existing training data immediately usable
4-phase implementation plan (Tracker → Lead Prediction → Adaptive Query + Abort → Config/Flags)
Ready for implementation on your confirmation. The plan is at @C:\Users\maksg\.windsurf\plans\adaptive-ailos-torc-69c5a5.md.

Feedback submitted


Your modified files:
importantConvo.txt
c:/Users/maksg/Downloads/HEAT_SACLOS
+434
backupPlan.nd
c:/Users/maksg/Downloads/HEAT_SACLOS
+356

"The existing CorrectionLearner.predict does three things that are destructive to impulsive TORC strokes:"
Does this mean that you're going to utilize K-1 from now on for static mode? If yes - this needs to be configurable for Mode: Static, Hyperparameter: NN/1 . 

Also, sometimes OCR returns incorrect range when measuring. One second it measures 30, and suddenly changes to 90, appends a digit or produces other artifact. When in dynamic mode and tracking is already active and corrects trajectory against moving target, we should assume no high amplitude range jumps and guard against that, a target can't suddenly shift by 50+ meters, or rather more precise and robust: the rate of change can't exceed a specific value, for safety it could be10-115 per second, best make it configurable and persist in existing config.

" If the tracker detects a velocity direction change >45° while the correction thread is executing, the thread is aborted and the correction re-triggers with an updated predicted_pos" aborting may mean that even for a short amount of time the mouse movement is interrupted until the new vector for the new thread is recalculated and new thread takes over. Would it be possible to inject into running thread instead of aborting?

Three distinct issues — let me assess each precisely.

Q1 — k=1 for static mode? No. The k=1 requirement is specific to adaptive mode. In static mode, k-blending is intentional and beneficial — it interpolates between training samples and improves generalization for the fixed-origin case. k=1 in static mode would make it brittle for edge-of-distribution inputs. However, making k configurable per mode is a legitimate hyperparameter exposure. Static mode gets static_nn_k in config (default: current behavior); adaptive mode hard-forces k=1.

Q2 — OCR range spike filtering Correct approach: rate-of-change clamp, not EMA. EMA would lag behind legitimate fast closure (the missile can close at up to ~320 m/s at late flight). Clamping rejects the outlier and holds the previous value without lag. Applies to both TORC and Smooth dynamic modes during playback. Configuration: ocr_range_max_rate_m_s.

Q3 — Thread injection vs abort Injection is feasible and better. The running correction thread checks a shared _pending_trajectory slot at each point in its inner loop. When the monitor thread detects a >45° vel_dir change, it computes the new trajectory (using current cursor position as origin), places it in the shared slot, and the running thread picks it up at its next iteration — no stop, no gap. The cursor transitions from the old stroke to the new trajectory mid-move with no interruption. For Smooth mode this doesn't apply — the P-controller tick already updates naturally.

adaptive-ailos-torc-69c5a5.md
+18
-5
  4. On detection: swaps to new trajectory at current cursor position, resets timing, continues
  - No mouse movement gap — cursor continues moving through the transition
  - The new trajectory origin is the cursor's actual position at swap time, so displacement is well-defined
  - **This is uncharted territory.** The missile has momentum from the partial first stroke; no training data models this transition. Empirical testing required.
 

Now adding the OCR range spike filter:

adaptive-ailos-torc-69c5a5.md
+15
- Applies to both Dynamic/TORC and Dynamic/Smooth during active correction
- Config: `ocr_range_max_rate_m_s` (default: 80, configurable range 10–150)
- Persisted in `saclos_config.json`
 
### Dynamic/Smooth mode

Plan is updated with all three changes. Summary of what was added:

k=1 scope — Static mode keeps configurable static_nn_k (default 3). Adaptive TORC hard-forces k=1. Smooth mode doesn't use KNN at all. Added as a table in the plan.

OCR range spike filter — Clamp-and-hold on |Δrange| / dt > ocr_range_max_rate_m_s. Default 80 m/s, range 10–150, persisted in saclos_config.json. Applied to both dynamic modes.

Thread injection — The correction thread never stops. Monitor thread pushes new trajectory into _pending_trajectory shared slot; running thread detects and swaps at its next step with current cursor position as the new origin. Zero gap in mouse movement.

The plan is at @C:\Users\maksg\.windsurf\plans\adaptive-ailos-torc-69c5a5.md and covers all open questions. Ready to proceed to implementation on your confirmation.

Feedback submitted


Your modified files:
saclos_config.json
c:/Users/maksg/Downloads/HEAT_SACLOS
+1
-1

"Q2 — OCR range spike filtering Correct approach: rate-of-change clamp, not EMA. EMA would lag behind legitimate fast closure (the missile can close at up to ~320 m/s at late flight). Clamping rejects the outlier and holds the previous value without lag. Applies to both TORC and Smooth dynamic modes during playback. Configuration: ocr_range_max_rate_m_s."
The range that OCR reads, is between operators tank and the target tank, it's not range from missile to target. I'm just making sure everything is clear. If your understanding was correct before - good.

My previous analysis was wrong in one specific way: I cited "missile closes at ~320 m/s" as the reason to avoid EMA — that applies to missile-to-target closure, but the OCR range is operator-to-target, which barely changes during the few seconds of missile flight. A target vehicle moves at maybe 5–20 m/s ground speed, so the legitimate range rate of change from the OCR is very low — far lower than I stated.

This actually makes the spike filter more effective, not less: legitimate range change per cycle is tiny (maybe 1–5 m at 5Hz OCR), so any OCR artifact (dropped digit, appended digit → 30–90m jump) is trivially detected and rejected. The clamping approach is still correct, but the default threshold should be much lower — 25–30 m/s is generous for any ground vehicle.

Updating the plan default:

adaptive-ailos-torc-69c5a5.md
+5
-3
- **Approach: clamp-and-hold**, not EMA. EMA lags even small legitimate changes; clamping rejects only clear outliers.
- Applies to both Dynamic/TORC and Dynamic/Smooth during active correction
- Config: `ocr_range_max_rate_m_s` (default: 80, configurable range 10–150)
- Config: `ocr_range_max_rate_m_s` (default: 25, configurable range 10–115)
- Persisted in `saclos_config.json`

Plan corrected. The revised default of 25 m/s is now appropriate given the actual physics — a ground vehicle closing or opening range at >25 m/s from a stationary operator position would be an unusually fast engagement, but it's still safely above typical target ground speeds so real readings won't get rejected.

Also worth noting: the spike filter at the fire-time snapshot (for t_flight calculation) is equally important, not just during playback. A bad OCR read at the moment you fire corrupts the lead prediction for the entire engagement. The filter should be active from the moment tracking begins, not only during correction playback.

adaptive-ailos-torc-69c5a5.md
+2
-1
- **Approach: clamp-and-hold**, not EMA. EMA lags even small legitimate changes; clamping rejects only clear outliers.
- Applies to both Dynamic/TORC and Dynamic/Smooth during active correction
- Active from the moment tracking begins — including at fire time (a bad OCR read at fire time corrupts the `t_flight` / `t_remaining` lead prediction for the entire engagement)
- Applies to both Dynamic/TORC and Dynamic/Smooth
- Config: `ocr_range_max_rate_m_s` (default: 25, configurable range 10–115)
Plan is current and complete. No open questions remain. Ready for implementation on your confirmation.

Feedback submitted


Your modified files:
importantConvo.txt
c:/Users/maksg/Downloads/HEAT_SACLOS
+136
-1
backupPlan.nd
c:/Users/maksg/Downloads/HEAT_SACLOS
+36
-5
saclos_config.json
c:/Users/maksg/Downloads/HEAT_SACLOS
+1
-1

Actually, apply that OCR rate of change gate to what comes out of it right away, so it's not specific only to Dynamic mode. Static mode also needs correct range readings. Since both use range, it's smart to modify the ocr reader itself

Correct call — the gate belongs in the reader, not in the consumers. Let me check the current OCR reader to understand where the range value is produced.

Good — the right place is clear from the code. There's already a TemporalOCRFilter class at @C:\Users\maksg\Downloads\HEAT_SACLOS\utils\ocr_reader.py:111-155 that handles single-frame artifact rejection. The rate-of-change gate belongs inside this class — it already tracks _last_confirmed and _last_confirmed_time, so the check is essentially:

python
if self._last_confirmed is not None and dt > 0:
    if abs(candidate - self._last_confirmed) / dt > max_rate_m_s:
        return self._last_confirmed  # rate-gate reject
This way every call to ocr_capture_range() — static mode, dynamic mode, training, predictor — gets filtered output. No consumers need changing.

The max_rate_m_s value needs to be injectable at construction time since it comes from config. The module-level _temporal_filter = TemporalOCRFilter() at line 158 needs to be replaced by a factory or the filter needs to expose a setter.

Updating the plan:

adaptive-ailos-torc-69c5a5.md
+18
-15
 
- Config: `ocr_range_max_rate_m_s` (default: 25, configurable range 10–115)
- Persisted in `saclos_config.json`
- No mode-specific logic needed; all consumers benefit automatically
 

Plan updated. The OCR rate gate is now scoped to utils/ocr_reader.py → TemporalOCRFilter.update(), with a configure_ocr_filter() helper called at startup from config. Static mode, dynamic mode, trainer, predictor — all get filtered values with zero per-consumer changes.

No open questions remain. Ready for implementation on your confirmation.

Feedback submitted



Actually, i was wrong to put as a gate on OCR itself, it would prevent significant value changes even when user is simply looking at different targets at different ranges. Instead this gate should be for consumers and active only when tracking and correction modes are active. The rate of change can't be high when tracking same target. 

Correct — putting it on the OCR reader would break the legitimate case where the user pans to a different target at a completely different range. The gate is only meaningful when locked onto a specific target, because that's when you know the range can't jump.

Reverting the OCR change and placing the gate in the tracking/correction subsystem instead:

adaptive-ailos-torc-69c5a5.md
+34
-20
- `utils/ocr_reader.py` unchanged
- Config: `ocr_range_max_rate_m_s` (default: 25, range 10–115), persisted in `saclos_config.json`
- Applies to both TORC and Smooth modes during active tracking only
- Transparent to all other callers (static mode, training, predictor) — they read OCR directly
 

Plan corrected. utils/ocr_reader.py is now left untouched. The TrackedRangeGate is a lightweight consumer-side object that activates only when a target designation is live — reset on designation start, filtering on every OCR tick during active tracking/correction, transparent to everything else.