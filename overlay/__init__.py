"""HEAT_SACLOS overlay packages.

* ``overlay.heat`` — universal modular HUD host with auto-discovered plugins.
* ``overlay.heat.plugins.heat_ailos_torc`` — ML-driven SACLOS predictor /
  trainer / refiner for ATGM guidance (FUSE plugin).
* ``overlay.ml.heat_ailos_torc`` — deprecated shim; redirects to the plugin.
"""
