"""HEAT_SACLOS overlay packages.

Two entry-point families:

* ``overlay.ml.heat_ailos_torc`` — ML-driven SACLOS predictor / trainer /
  refiner for ATGM guidance. Profile-selectable at startup.
* ``overlay.heat`` — universal modular HUD host with auto-discovered plugins.
"""
