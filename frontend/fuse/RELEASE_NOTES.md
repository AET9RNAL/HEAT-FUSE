## FUSE 1.7.0

**NEW**

- **Prism** plugin - fine tune the native game HUD. Recolour, scale and rotate UI elements.

**IMPROVEMENTS**

- Runtime 4.5.0: new `color` config entry type for plugins - `#RRGGBBAA` values with an optional alpha toggle.
- Runtime: Rive overlays now render through the WebGL2 backend.
